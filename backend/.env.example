# Server port (Render sets this automatically)
PORT=5000

# Set to "true" on Render to use persistent disk path
RENDER=true

# Your Vercel frontend URL (for CORS)
FRONTEND_URL=https://your-app.vercel.app

# Node version
NODE_VERSION=20
